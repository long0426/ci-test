apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
  labels:
    app: svc-service-registry
    app.kubernetes.io/component: web
    app.kubernetes.io/instance: service-registry
    app.kubernetes.io/name: service-registry
    app.kubernetes.io/part-of: service-registry
    app.openshift.io/runtime: java
  name: svc-service-registry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: svc-service-registry
  template:
    metadata:
      labels:
        app: svc-service-registry
    spec:
      containers:
        - name: svc-service-registry
          imagePullPolicy: Always
          image: image-registry.openshift-image-registry.svc:5000/message_on_kafka/cpa:5d5ccca20dd6300e0747622af2ce3c7003b2ea76
          ports:
            - containerPort: 8582
              protocol: TCP